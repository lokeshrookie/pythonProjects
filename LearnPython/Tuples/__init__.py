"""
    Tuples are  to like arrays in Java.
    # Tuples are immutable where lists are immutable
"""

tup = (1, 2, 3, 4, 5)
tup.count(3)  # Counts the no of duplicates of a given element
print(tup.count(5))
print(tup.index(5))     # Prints the index of a given element
